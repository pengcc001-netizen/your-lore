import { useState, useEffect } from 'react' import { Helmet } from 'react-helmet-async' import { useParams, Link } from 'react-router-dom' import { quizQuestions } from '../data/questions' import { archetypes } from '../data/archetypes' import Ad from '../components/Ad' import ShareButtons from '../components/ShareButtons'  const STAT_KEYS = ['strength', 'wisdom', 'charisma', 'cunning', 'resilience', 'magic'] as const  function getArchetype(totalStats: Record<string, number>): typeof archetypes[0] {   const dominant = STAT_KEYS.reduce((max, key) => totalStats[key] > totalStats[max] ? key : max, 'strength')   const archetypeMap: Record<string, string> = { strength: 'the-warlord', wisdom: 'the-mentor', charisma: 'the-sovereign', cunning: 'the-trickster', resilience: 'the-exile', magic: 'the-oracle' }   const slug = archetypeMap[dominant]   return archetypes.find(a => a.slug === slug) || archetypes[0] }  export default function ResultPage() {   const { code } = useParams<{ code: string }>()   const [decoded, setDecoded] = useState<number[] | null>(null)    useEffect(() => {     try {       const std = (code || '').replace(/-/g, '+').replace(/_/g, '/')       const padded = std + '='.repeat((4 - std.length % 4) % 4)       const parsed = JSON.parse(atob(padded))       if (Array.isArray(parsed) && parsed.length === quizQuestions.length) setDecoded(parsed)     } catch { setDecoded(null) }   }, [code])    if (!decoded) {     return (       <div style={{ textAlign: 'center', padding: '60px 0' }}>         <Helmet><meta name="robots" content="noindex,nofollow" /><title>404 - Not Found | Your Lore</title></Helmet>         <p style={{ color: 'var(--text-soft)', fontFamily: 'var(--font-mono)' }}>Invalid lore link.</p>         <Link to="/" className="btn btn-gold" style={{ marginTop: 16 }}>Take the Quiz</Link>       </div>     )   }    const totalStats: Record<string, number> = { strength: 0, wisdom: 0, charisma: 0, cunning: 0, resilience: 0, magic: 0 }   decoded.forEach((optionIndex, i) => {     const opt = quizQuestions[i].options[optionIndex]     Object.entries(opt.stats).forEach(([k, v]) => { totalStats[k] = (totalStats[k] || 0) + (v || 0) })   })   const archetype = getArchetype(totalStats)   const maxStat = Math.max(...Object.values(totalStats))   const shareUrl = typeof window !== 'undefined' ? window.location.href : 'https://lore.csskey.com'    return (     <div className="fade-in" style={{ maxWidth: 680, margin: '0 auto' }}>       <Helmet><title>{archetype.name} - Character Lore | Your Lore</title><meta name="robots" content="noindex,nofollow" />       <meta property="og:title" content={`My Character Archetype is ${archetype.name}!`} />       <meta property="og:description" content={archetype.tagline} />       <meta property="og:url" content={shareUrl} />       <meta property="og:type" content="website" />       <meta name="twitter:card" content="summary_large_image" />       <meta name="twitter:title" content={`My Character: ${archetype.name}`} />       <meta name="twitter:description" content={archetype.tagline} />       </Helmet>       <div className="gold-card gold-card-ornate" style={{ padding: '40px 32px', marginBottom: 32 }}>         <div style={{ textAlign: 'center', marginBottom: 32 }}>           <div className="mono" style={{ fontSize: 11, letterSpacing: '0.3em', color: 'var(--gold)', marginBottom: 8 }}>SHARED CHARACTER SHEET</div>           <div style={{ width: 60, height: 1, background: 'var(--gold)', margin: '0 auto 24px', opacity: 0.5 }} />         </div>         <div style={{ textAlign: 'center', marginBottom: 32 }}>           <div style={{ fontSize: '4rem', marginBottom: 16 }}>{archetype.emoji}</div>           <h1 className="gradient-text" style={{ fontFamily: 'var(--font-heading)', fontSize: 'clamp(2rem, 5vw, 2.75rem)', fontWeight: 800, marginBottom: 8 }}>{archetype.name}</h1>           <p style={{ fontSize: 16, color: 'var(--gold)', fontStyle: 'italic', fontFamily: 'var(--font-body)' }}>"{archetype.tagline}"</p>         </div>         <div style={{ marginBottom: 24 }}>           <div className="mono" style={{ fontSize: 11, letterSpacing: '0.2em', color: 'var(--text-faint)', marginBottom: 12 }}>ATTRIBUTES</div>           {STAT_KEYS.map(key => (             <div key={key} style={{ marginBottom: 12 }}>               <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>                 <span className="mono" style={{ fontSize: 12, color: 'var(--text-soft)', textTransform: 'capitalize' }}>{key}</span>                 <span className="mono" style={{ fontSize: 12, color: 'var(--gold)', fontWeight: 600 }}>{totalStats[key]}</span>               </div>               <div style={{ height: 6, background: 'var(--border)', borderRadius: 3, overflow: 'hidden' }}>                 <div style={{ height: '100%', width: `${(totalStats[key] / maxStat) * 100}%`, background: 'linear-gradient(90deg, var(--gold-dark), var(--gold))', borderRadius: 3 }} />               </div>             </div>           ))}         </div>         <div style={{ marginBottom: 24, paddingTop: 24, borderTop: '1px solid var(--border)' }}>           <div className="eyebrow" style={{ marginBottom: 12 }}>Backstory</div>           {archetype.backstory.split('\n\n').map((para, i) => (             <p key={i} style={{ fontSize: 15, lineHeight: 1.8, color: 'var(--text)', fontFamily: 'var(--font-body)', textAlign: 'justify', marginBottom: i < archetype.backstory.split('\n\n').length - 1 ? '1em' : 0 }}>{para}</p>           ))}         </div>         <div style={{ marginBottom: 24, paddingTop: 24, borderTop: '1px solid var(--border)' }}>           <div className="eyebrow" style={{ marginBottom: 12 }}>Abilities</div>           <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>{archetype.abilities.map((ab, i) => <span key={i} className="chip chip-gold">{ab}</span>)}</div>         </div>         <div style={{ paddingTop: 24, borderTop: '1px solid var(--border)' }}>           <div className="eyebrow" style={{ marginBottom: 8, color: 'var(--danger-bright)' }}>Fatal Flaw</div>           <p style={{ fontSize: 14, lineHeight: 1.7, color: 'var(--text)', fontFamily: 'var(--font-body)', fontStyle: 'italic' }}>{archetype.fatalFlaw}</p>         </div>       </div>       <div style={{ textAlign: 'center', marginBottom: 32 }}><ShareButtons url={shareUrl} title={`My character lore is ${archetype.name}! Discover yours:`} /></div>       <Ad />       <div style={{ textAlign: 'center', marginBottom: 40 }}>         <Link to="/" className="btn btn-gold">Discover Your Own Lore 鈫?/Link>       </div>     </div>   ) }