import { useState } from 'react' import { Helmet } from 'react-helmet-async' import { Link, useNavigate } from 'react-router-dom' import { quizQuestions } from '../data/questions' import { archetypes } from '../data/archetypes' import Ad from '../components/Ad'  type Phase = 'intro' | 'quiz' | 'result' interface Answer { questionId: number; optionIndex: number; stats: { strength?: number; wisdom?: number; charisma?: number; cunning?: number; resilience?: number; magic?: number } }  const STAT_KEYS = ['strength', 'wisdom', 'charisma', 'cunning', 'resilience', 'magic'] as const  function getArchetype(totalStats: Record<string, number>): typeof archetypes[0] {   const dominant = STAT_KEYS.reduce((max, key) => totalStats[key] > totalStats[max] ? key : max, 'strength')   const archetypeMap: Record<string, string> = { strength: 'the-warlord', wisdom: 'the-mentor', charisma: 'the-sovereign', cunning: 'the-trickster', resilience: 'the-exile', magic: 'the-oracle' }   const slug = archetypeMap[dominant]   return archetypes.find(a => a.slug === slug) || archetypes[0] }  export default function Home() {   const [phase, setPhase] = useState<Phase>('intro')   const [currentQ, setCurrentQ] = useState(0)   const [answers, setAnswers] = useState<Answer[]>([])   const navigate = useNavigate()    const handleAnswer = (optionIndex: number) => {     const q = quizQuestions[currentQ]     const opt = q.options[optionIndex]     const newAnswers = [...answers, { questionId: q.id, optionIndex, stats: opt.stats }]     setAnswers(newAnswers)     if (currentQ < quizQuestions.length - 1) {       setCurrentQ(currentQ + 1)     } else {       const totalStats: Record<string, number> = { strength: 0, wisdom: 0, charisma: 0, cunning: 0, resilience: 0, magic: 0 }       newAnswers.forEach(a => { Object.entries(a.stats).forEach(([k, v]) => { totalStats[k] = (totalStats[k] || 0) + (v || 0) }) })       const encoded = btoa(JSON.stringify(newAnswers.map(a => a.optionIndex))).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '')       window.history.replaceState(null, '', `/r/${encoded}`)       setPhase('result')     }   }    const resetQuiz = () => { setPhase('intro'); setCurrentQ(0); setAnswers([]); navigate('/') }    if (phase === 'intro') {     return (       <div className="fade-in">         <Helmet>           <title>Your Lore - Discover Your Character Backstory [2026]</title>           <meta name="description" content="Take the quiz and discover your character lore. 8 archetypes, unique backstories, abilities, fatal flaws, and quests. Free, no sign-up." />           <meta property="og:title" content="Your Lore - Discover Your Character Archetype" />           <meta property="og:description" content="Discover your character archetype. 8 archetypes, 40 abilities, an epic personality quiz." />           <meta property="og:url" content="https://lore.csskey.com/" />           <meta property="og:type" content="website" />           <meta name="twitter:card" content="summary" />           <script type="application/ld+json">{JSON.stringify({ "@context": "https://schema.org", "@type": "WebApplication", "name": "Your Lore", "url": "https://lore.csskey.com/", "applicationCategory": "EntertainmentApplication", "operatingSystem": "Any", "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }, "description": "Discover your character lore. Take the quiz and uncover your epic backstory, abilities, and quest." })}</script>         </Helmet>         <section style={{ textAlign: 'center', padding: '40px 0 60px' }}>           <div className="eyebrow" style={{ marginBottom: 16 }}>Character Lore Generator 路 2026</div>           <h1 className="gradient-text" style={{ fontSize: 'clamp(3rem, 8vw, 5.5rem)', fontWeight: 900, fontFamily: 'var(--font-heading)', letterSpacing: '-0.03em', lineHeight: 1, marginBottom: 24 }}>             Discover Your<br />Character Lore           </h1>           <p style={{ fontSize: 'clamp(1rem, 2vw, 1.25rem)', color: 'var(--text-soft)', maxWidth: 560, margin: '0 auto 40px', lineHeight: 1.7, fontFamily: 'var(--font-body)' }}>             Answer 12 questions and uncover your epic backstory, abilities, fatal flaw, signature item, and quest. Which of {archetypes.length} archetypes are you?           </p>           <button onClick={() => setPhase('quiz')} className="btn btn-gold" style={{ padding: '16px 48px', fontSize: 14 }}>Begin Your Journey 鈫?/button>           <div style={{ marginTop: 16 }}><span style={{ color: 'var(--text-faint)', fontSize: 12, fontFamily: 'var(--font-mono)' }}>12 questions 路 ~2 minutes 路 Free, no sign-up</span></div>         </section>         <section style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(160px, 1fr))', gap: 16, marginBottom: 60 }}>           {[{ label: 'Character Archetypes', value: `${archetypes.length}` }, { label: 'Unique Abilities', value: '40+' }, { label: 'Stat Categories', value: '6' }, { label: 'Questions', value: '12' }].map((stat, i) => (             <div key={i} className="gold-card" style={{ padding: '24px 20px', textAlign: 'center' }}>               <div className="mono" style={{ fontSize: '1.75rem', fontWeight: 700, color: 'var(--gold)', marginBottom: 4 }}>{stat.value}</div>               <div className="section-label" style={{ fontSize: 11 }}>{stat.label}</div>             </div>           ))}         </section>         <Ad />         <section style={{ marginBottom: 60 }}>           <div className="eyebrow" style={{ textAlign: 'center', marginBottom: 8 }}>How It Works</div>           <h2 style={{ textAlign: 'center', fontSize: 'clamp(1.5rem, 3vw, 2rem)', fontFamily: 'var(--font-heading)', marginBottom: 40 }}>Three Steps to Your Character Sheet</h2>           <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: 24 }}>             {[{ num: '01', title: 'Answer 12 Questions', desc: 'Tell us how you handle destiny, power, betrayal, and the wilderness.' }, { num: '02', title: 'Get Your Stats', desc: 'We calculate your Strength, Wisdom, Charisma, Cunning, Resilience, and Magic.' }, { num: '03', title: 'Read Your Lore', desc: 'Receive your full character sheet: backstory, abilities, fatal flaw, signature item, and quest.' }].map((step, i) => (               <div key={i} className="gold-card" style={{ padding: '32px 28px' }}>                 <div className="mono" style={{ fontSize: '2rem', fontWeight: 700, color: 'var(--gold)', opacity: 0.3, marginBottom: 8 }}>{step.num}</div>                 <h3 style={{ fontFamily: 'var(--font-heading)', fontSize: '1.25rem', marginBottom: 12, color: 'var(--text)' }}>{step.title}</h3>                 <p style={{ color: 'var(--text-soft)', fontSize: 15, lineHeight: 1.7, fontFamily: 'var(--font-body)' }}>{step.desc}</p>               </div>             ))}           </div>         </section>         <section>           <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline', marginBottom: 24 }}>             <div><div className="eyebrow" style={{ marginBottom: 4 }}>Featured</div><h2 style={{ fontFamily: 'var(--font-heading)', fontSize: 'clamp(1.5rem, 3vw, 2rem)' }}>The Archetypes</h2></div>             <Link to="/archetypes" style={{ color: 'var(--gold)', fontSize: 14, fontFamily: 'var(--font-mono)', fontWeight: 600 }}>View All 鈫?/Link>           </div>           <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: 16 }}>             {archetypes.slice(0, 4).map((a) => (               <Link key={a.slug} to={`/archetypes/${a.slug}`} style={{ textDecoration: 'none' }}>                 <div className="gold-card" style={{ padding: '24px', height: '100%' }}>                   <div style={{ fontSize: '2rem', marginBottom: 8 }}>{a.emoji}</div>                   <h3 style={{ fontFamily: 'var(--font-heading)', fontSize: '1.125rem', fontWeight: 700, color: 'var(--text)', marginBottom: 4 }}>{a.name}</h3>                   <p style={{ fontSize: 13, color: 'var(--gold)', fontStyle: 'italic', fontFamily: 'var(--font-body)' }}>"{a.tagline}"</p>                 </div>               </Link>             ))}           </div>         </section>       </div>     )   }    if (phase === 'quiz') {     const q = quizQuestions[currentQ]     const progress = ((currentQ + 1) / quizQuestions.length) * 100     return (       <div className="fade-in" style={{ maxWidth: 720, margin: '0 auto' }}>         <Helmet><title>Character Quiz - Question {currentQ + 1} of {quizQuestions.length} | Your Lore</title></Helmet>         <div style={{ marginBottom: 40 }}>           <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>             <span className="mono" style={{ fontSize: 12, color: 'var(--text-soft)', letterSpacing: '0.1em' }}>QUESTION {currentQ + 1} / {quizQuestions.length}</span>             <span className="mono" style={{ fontSize: 12, color: 'var(--gold)' }}>{Math.round(progress)}%</span>           </div>           <div style={{ height: 4, background: 'var(--border)', borderRadius: 2, overflow: 'hidden' }}>             <div style={{ height: '100%', width: `${progress}%`, background: 'linear-gradient(90deg, var(--gold-dark), var(--gold-bright))', borderRadius: 2, transition: 'width 0.4s ease' }} />           </div>         </div>         <h2 style={{ fontFamily: 'var(--font-heading)', fontSize: 'clamp(1.5rem, 3vw, 2rem)', fontWeight: 700, lineHeight: 1.3, marginBottom: 32, color: 'var(--text)' }}>{q.question}</h2>         <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>           {q.options.map((opt, i) => (             <button key={i} onClick={() => handleAnswer(i)} style={{ background: 'var(--bg-card)', border: '1px solid var(--border)', borderRadius: 'var(--radius)', padding: '20px 24px', textAlign: 'left', cursor: 'pointer', transition: 'all 0.2s ease', color: 'var(--text)', fontSize: 15, fontFamily: 'var(--font-body)', lineHeight: 1.5 }}               onMouseEnter={(e) => { e.currentTarget.style.borderColor = 'var(--gold)'; e.currentTarget.style.background = 'var(--gold-soft)' }} onMouseLeave={(e) => { e.currentTarget.style.borderColor = 'var(--border)'; e.currentTarget.style.background = 'var(--bg-card)' }}>               <span style={{ color: 'var(--gold)', fontFamily: 'var(--font-mono)', fontWeight: 600, marginRight: 12 }}>{String.fromCharCode(65 + i)}</span>{opt.text}             </button>           ))}         </div>         {currentQ > 0 && <div style={{ marginTop: 24, textAlign: 'center' }}><button onClick={() => { setCurrentQ(currentQ - 1); setAnswers(answers.slice(0, -1)) }} style={{ background: 'none', border: 'none', color: 'var(--text-faint)', cursor: 'pointer', fontSize: 13, fontFamily: 'var(--font-mono)' }}>鈫?Previous question</button></div>}       </div>     )   }    if (phase === 'result') {     const totalStats: Record<string, number> = { strength: 0, wisdom: 0, charisma: 0, cunning: 0, resilience: 0, magic: 0 }     answers.forEach(a => { Object.entries(a.stats).forEach(([k, v]) => { totalStats[k] = (totalStats[k] || 0) + (v || 0) }) })     const archetype = getArchetype(totalStats)     const shareUrl = typeof window !== 'undefined' ? window.location.href : 'https://lore.csskey.com'     const maxStat = Math.max(...Object.values(totalStats))      return (       <div className="fade-in" style={{ maxWidth: 680, margin: '0 auto' }}>         <Helmet><title>Your Lore: {archetype.name} | Your Lore</title><meta name="description" content={`My character lore: ${archetype.name}. ${archetype.tagline} Discover yours!`} /><meta name="robots" content="noindex,nofollow" /></Helmet>         <div className="gold-card gold-card-ornate" style={{ padding: '40px 32px', marginBottom: 32 }}>           <div style={{ textAlign: 'center', marginBottom: 32 }}>             <div className="mono" style={{ fontSize: 11, letterSpacing: '0.3em', color: 'var(--gold)', marginBottom: 8 }}>CHARACTER SHEET</div>             <div style={{ width: 60, height: 1, background: 'var(--gold)', margin: '0 auto 24px', opacity: 0.5 }} />           </div>           <div style={{ textAlign: 'center', marginBottom: 32 }}>             <div style={{ fontSize: '4rem', marginBottom: 16 }}>{archetype.emoji}</div>             <h1 className="gradient-text" style={{ fontFamily: 'var(--font-heading)', fontSize: 'clamp(2rem, 5vw, 2.75rem)', fontWeight: 800, marginBottom: 8 }}>{archetype.name}</h1>             <p style={{ fontSize: 16, color: 'var(--gold)', fontStyle: 'italic', fontFamily: 'var(--font-body)' }}>"{archetype.tagline}"</p>           </div>           {/* Stats */}           <div style={{ marginBottom: 32 }}>             <div className="mono" style={{ fontSize: 11, letterSpacing: '0.2em', color: 'var(--text-faint)', marginBottom: 12 }}>ATTRIBUTES</div>             {STAT_KEYS.map(key => (               <div key={key} style={{ marginBottom: 12 }}>                 <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 4 }}>                   <span className="mono" style={{ fontSize: 12, color: 'var(--text-soft)', textTransform: 'capitalize' }}>{key}</span>                   <span className="mono" style={{ fontSize: 12, color: 'var(--gold)', fontWeight: 600 }}>{totalStats[key]}</span>                 </div>                 <div style={{ height: 6, background: 'var(--border)', borderRadius: 3, overflow: 'hidden' }}>                   <div style={{ height: '100%', width: `${(totalStats[key] / maxStat) * 100}%`, background: 'linear-gradient(90deg, var(--gold-dark), var(--gold))', borderRadius: 3, transition: 'width 0.5s ease' }} />                 </div>               </div>             ))}           </div>           {/* Backstory */}           <div style={{ marginBottom: 24, paddingTop: 24, borderTop: '1px solid var(--border)' }}>             <div className="eyebrow" style={{ marginBottom: 12 }}>Backstory</div>             {archetype.backstory.split('\n\n').map((para, i) => (               <p key={i} className={i === 0 ? 'dropcap' : ''} style={{ fontSize: 15, lineHeight: 1.8, color: 'var(--text)', fontFamily: 'var(--font-body)', textAlign: 'justify', marginBottom: i < archetype.backstory.split('\n\n').length - 1 ? '1em' : 0 }}>{para}</p>             ))}           </div>           {/* Abilities */}           <div style={{ marginBottom: 24, paddingTop: 24, borderTop: '1px solid var(--border)' }}>             <div className="eyebrow" style={{ marginBottom: 12 }}>Abilities</div>             <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>{archetype.abilities.map((ab, i) => <span key={i} className="chip chip-gold">{ab}</span>)}</div>           </div>           {/* Fatal Flaw */}           <div style={{ marginBottom: 24, paddingTop: 24, borderTop: '1px solid var(--border)' }}>             <div className="eyebrow" style={{ marginBottom: 8, color: 'var(--danger-bright)' }}>Fatal Flaw</div>             <p style={{ fontSize: 14, lineHeight: 1.7, color: 'var(--text)', fontFamily: 'var(--font-body)', fontStyle: 'italic' }}>{archetype.fatalFlaw}</p>           </div>           {/* Signature Item */}           <div style={{ marginBottom: 24, paddingTop: 24, borderTop: '1px solid var(--border)' }}>             <div className="eyebrow" style={{ marginBottom: 8 }}>Signature Item</div>             <p style={{ fontSize: 14, lineHeight: 1.7, color: 'var(--text)', fontFamily: 'var(--font-body)' }}>{archetype.signatureItem}</p>           </div>           {/* Quest */}           <div style={{ paddingTop: 24, borderTop: '1px solid var(--border)' }}>             <div className="eyebrow" style={{ marginBottom: 8 }}>Your Quest</div>             <p style={{ fontSize: 14, lineHeight: 1.7, color: 'var(--text)', fontFamily: 'var(--font-body)' }}>{archetype.quest}</p>           </div>         </div>         <div style={{ textAlign: 'center', marginBottom: 32 }}>           <div className="section-label" style={{ marginBottom: 16 }}>Share Your Lore</div>           <div style={{ display: 'flex', gap: 10, justifyContent: 'center', flexWrap: 'wrap' }}>             <a href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(`My character lore is ${archetype.name}! Discover yours:`)}&url=${encodeURIComponent(shareUrl)}`} target="_blank" rel="noopener" className="btn btn-outline" style={{ fontSize: 12 }}>Share on X</a>             <a href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(shareUrl)}`} target="_blank" rel="noopener" className="btn btn-outline" style={{ fontSize: 12 }}>Share on Facebook</a>             <button onClick={() => navigator.clipboard.writeText(shareUrl)} className="btn btn-outline" style={{ fontSize: 12 }}>Copy Link</button>           </div>         </div>         <Ad />         <div style={{ textAlign: 'center', marginBottom: 40 }}>           <button onClick={resetQuiz} className="btn btn-ghost" style={{ fontSize: 13 }}>鈫?Retake the Quiz</button>         </div>         <div style={{ textAlign: 'center' }}>           <div className="section-label" style={{ marginBottom: 16 }}>Explore More</div>           <div style={{ display: 'flex', gap: 12, justifyContent: 'center', flexWrap: 'wrap' }}>             <Link to="/archetypes" className="btn btn-outline" style={{ fontSize: 12 }}>All Archetypes</Link>             <Link to="/blog" className="btn btn-outline" style={{ fontSize: 12 }}>Read the Lorekeeper</Link>           </div>         </div>       </div>     )   }   // Defensive fallback 鈥?should never be reached   return (     <div className="fade-in" style={{ minHeight: '60vh', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 16 }}>       <Helmet>         <title>Page Not Found - Your Lore</title>         <meta name="robots" content="noindex,nofollow" />       </Helmet>       <h1 style={{ fontSize: 48, fontWeight: 800 }}>404</h1>       <p>Something went wrong. Please try again.</p>       <Link to="/" style={{ textDecoration: 'none', fontWeight: 600, color: 'var(--accent)' }}>Go Home</Link>     </div>   ) }